name: CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

permissions:
  contents: read

jobs:
  ci:
    uses: Projet-technique-Altair/altair-ci/.github/workflows/lab-api-ci.yml@main
